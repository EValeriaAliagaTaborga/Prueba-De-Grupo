<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="titaniumstudios.myapplication2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MenuHamburguesaActivity" />
        <activity android:name=".ArmadoActivity" />
        <activity android:name=".ArmaloActivity" />
        <activity android:name=".ArmadoActivity" />
        <activity android:name=".VerificarFacturaActivity" />
        <activity android:name=".DatosYFacturacionActivity" />
        <activity android:name=".VerificacionDePedidoActivity" />
        <activity android:name=".GraciasActivity"></activity>
    </application>

</manifest>